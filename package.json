{
  "name": "testcurl",
  "version": "1.0.0",
  "description": "script that scrapes a site and saves the output to a json file",  
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",    
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.2.2",
    "cheerio": "^1.0.0-rc.12"
  }
}
